# 回報：T-001 game-engineer（S2 engine：reducer ＋ seeded RNG ＋ AI）

> 補寫說明：S2 由前一位 game-engineer 完成但未寫回報（被中斷）。本檔由接手的 game-engineer 依現有檔案與實際指令輸出如實補寫。

| 欄位 | 內容 |
|---|---|
| 角色 | game-engineer |
| 任務 | T-001 S2 engine |
| 日期 | 2026-09-13（補寫） |
| 結論 | 完成 |

## 1. 結論（3 行內）

- `src/engine/{rng,selectors,ai,reducer,index}.js` 存在，CommonJS，純函數；`reduce` 對前置條件不成立的 action 拋 `Error("REJECT:<TYPE>:<reason>")`；`AI_TURN` 一次只走一步；state 形狀與規格 §3.1 一致（`T-INV-schema-keys` 通過）。
- 附錄 A 兩項拍板已實作：`START_GAME` 未帶 `aiJobId` 時以 rng 隨機三選一（洗牌前消耗 1 次 RNG）；seed 存於 state 供 UI 顯示。
- `.claude/hooks/check-forbidden.sh` 對 `src/engine/` 零命中；`npm run test:engine` 全綠。

## 2. 變更檔案清單

| 檔案（相對路徑） | 動作 | 行數 ＋／－ | 備份檔 |
|---|---|---|---|
| `src/engine/rng.js` | 新增 | +29 | — |
| `src/engine/selectors.js` | 新增 | +92 | — |
| `src/engine/ai.js` | 新增 | +72 | — |
| `src/engine/reducer.js` | 新增 | +405 | — |
| `src/engine/index.js` | 新增 | +22 | — |
| `tests/engine/rng.test.js` | 新增 | +64 | — |
| `tests/engine/replay.test.js` | 新增 | +38 | — |
| `package.json` | 新增（只放 `test:engine`、`private: true`） | +12 | — |

實際匯出（供 UI／測試對照）：
- `rng.js`：`{ seedRng, next, nextInt }`
- `selectors.js`：`{ byId, ownedAssets, assetsWithEffect, netWorth, threshold, candidates, bagSummary, bagBlackSwanCount }`（比分派單多 `ownedAssets`、`assetsWithEffect`、`bagBlackSwanCount`）
- `ai.js`：`{ decideAi, decideFor }`（`decideFor` 供測試以同策略代打 human 座位）
- `reducer.js`：`{ initialState, createInitialState, reduce, canApply, SPEC_VERSION }`
- `index.js`：以上總表。

## 3. 測試證據

- 指令：`npm run test:engine`；`bash .claude/hooks/check-forbidden.sh`
- 輸出摘要（S2 最少要求的測試）：

```
T-RNG-01-mulberry32-vector ok
T-RNG-02-call-count ok
T-R-21-replay-deterministic ok
T-R-25-reducer-immutable ok
T-R-07-bust-halves ok
hook：零命中（exit 0）
```

- 未測試的項目與原因：分派單列的 `T-R-19-ai-stop-rules`、`T-R-20-ai-buy-most-expensive` **在現有測試檔中查無同名測試**（見 S3 回報 §4）；AI 規則目前只由 `tests/engine/selfplay.js` 的自動對局間接覆蓋。

## 4. 未預期發現

- A-04（寫死平衡數字）：`src/engine/` 內只出現 0、1、−1 與 mulberry32 的演算法常數（rng.js 有註明非平衡數值），符合契約。
- `reducer.js` 的 `createInitialState` 與 `initialState` 為同一函數的兩個名字，可能是為了相容分派單與 ADR 的不同稱呼；無害。
- selfplay 統計（`docs/reports/T-001-S3-selfplay.md`）顯示 AI 對 AI 爆倉率 0%：因 `swanStopOffset ≥ 1` 使 AI 永遠在門檻前一顆停手。這是規格 §7 的設計結果，非 bug；但 easy 難度 AI 勝率僅 0.5%、hard 92%，難度差距極大，建議 G5 playtest 觀察。

## 5. 需要製作人決定的事

- 無（難度曲線留 G5）。
