# 回報：T-001 game-engineer（S1 資料檔）

> 補寫說明：S1 由前一位 game-engineer 完成但未寫回報（被中斷）。本檔由接手的 game-engineer 於 S4／S5 期間依現有檔案與實際指令輸出如實補寫，不補記當時的工作過程。

| 欄位 | 內容 |
|---|---|
| 角色 | game-engineer |
| 任務 | T-001 S1 資料檔 |
| 日期 | 2026-09-13（補寫） |
| 結論 | 完成 |

## 1. 結論（3 行內）

- `src/data/{jobs,assets,tokens,balance}.json` 四檔存在、皆為合法 JSON，頂層 key 為 `jobs`／`assets`／`tokens`，`balance.json` 頂層即物件，與規格書 §6.1–6.4 一致。
- 資料驗證測試 `T-DATA-00`～`T-DATA-08`（tests/engine/data.test.js）全部通過，涵蓋分派單要求的 T-DATA-01～06。
- 無阻礙。

## 2. 變更檔案清單

| 檔案（相對路徑） | 動作 | 行數 ＋／－ | 備份檔 |
|---|---|---|---|
| `src/data/jobs.json` | 新增 | +28 | — |
| `src/data/assets.json` | 新增 | +16 | — |
| `src/data/tokens.json` | 新增 | +11 | — |
| `src/data/balance.json` | 新增 | +18 | — |

## 3. 測試證據

- 指令：`node -e 'for (const f of ["jobs","assets","tokens","balance"]) console.log(f, Object.keys(require("./src/data/"+f+".json")))'`（來源：分派單 S1 輸出契約）
- 指令：`npm run test:engine`（CLAUDE.md 第 4 節，S5 已回填）
- 輸出摘要：

```
T-DATA-00-json-parse ok
T-DATA-01-asset-ids-unique ok        （12 張、id 唯一）
T-DATA-02-job-bag-tokens-exist ok
T-DATA-03-tokens-added-exist ok
T-DATA-04-effect-enum ok             （ADD_TOKENS／SWAN_RETURN_ONCE／STOP_BONUS／NTH_INCOME_DOUBLE／THRESHOLD_PLUS）
T-DATA-05-ai-difficulties ok         （easy／normal／hard）
T-DATA-06-rule-cards-at-least-3 ok   （a07、a08、a09、a10 共 4 張非 ADD_TOKENS）
T-DATA-07-engine-never-reads-ui-balance ok
T-DATA-08-no-simplified-chinese ok
```

- 手算對照（分派單要求）：資產 12 張、職業 3 張、籌碼 6 種；`startBag` 的 key 只出現 `income_s`／`income_m`／`income_l`／`blackSwan`，皆存在於 tokens.json；`balance.ai` 恰三個 key。
- 未測試的項目與原因：無。

## 4. 未預期發現

- `docs/ui/v1-C.html` 的假資料（門檻 4、滷味攤起始 $120、袋子 8 顆）與正式資料不同（門檻 3、滷味攤 $80、11 顆）；S4 已一律讀 state／GAME_DATA，不沿用假資料，僅提醒 ui-designer 之後更新靜態稿時對齊。
- `balance.json` 含 `purchasesPerTurn: 1`，但 reducer 以 `purchasedThisTurn` 布林實作「每回合限購一次」，並未讀取此欄位；UI 有讀它來顯示「0–1 張」。若未來要改成可買多張，engine 需同步改。

## 5. 需要製作人決定的事

- 無。
