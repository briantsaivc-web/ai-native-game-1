# 回報：T-001 game-engineer（S3 engine 測試補齊）

> 補寫說明：S3 由前一位 game-engineer 進行到一半被中斷、未寫回報。本檔由接手的 game-engineer 依現有測試檔與實際輸出如實補寫；**未補寫任何 engine 測試**（接手範圍只到 tests/ui 與 tests/engine/build.test.js）。

| 欄位 | 內容 |
|---|---|
| 角色 | game-engineer |
| 任務 | T-001 S3 engine 測試補齊 |
| 日期 | 2026-09-13（補寫） |
| 結論 | 部分完成 |

## 1. 結論（3 行內）

- `tests/engine/` 現有 5 個測試檔共 **43 個測試全部通過**（`npm run test:engine`；加上 S5 新增的 2 個 T-BUILD 後為 45）。
- 規格 §5 的 25 個 `T-R-*` 中已有 22 個同名測試；**缺 `T-R-18`、`T-R-19`、`T-R-20`（AI 規則）**，且分派單要求的 `tests/engine/ai.test.js` 與 `T-AI-selfplay-stats` 測試不存在（只有可手動執行的 `tests/engine/selfplay.js`）。
- 自動對局統計已產出：`docs/reports/T-001-S3-selfplay.md`（`node tests/engine/selfplay.js`，每格 200 局）。

## 2. 變更檔案清單

| 檔案（相對路徑） | 動作 | 行數 ＋／－ | 備份檔 |
|---|---|---|---|
| `tests/engine/helpers.js` | 新增 | +123 | — |
| `tests/engine/rules.test.js` | 新增 | +394 | — |
| `tests/engine/invariants.test.js` | 新增 | +122 | — |
| `tests/engine/data.test.js` | 新增 | +122 | — |
| `tests/engine/selfplay.js` | 新增（統計腳本，非 node:test） | +123 | — |
| `docs/reports/T-001-S3-selfplay.md` | 新增（接手者以 `node tests/engine/selfplay.js > …` 產生） | — | — |

## 3. 測試證據

- 指令：`npm run test:engine`
- 輸出摘要：

```
# tests 43   # pass 43   # fail 0   # skipped 0   （S5 加入 T-BUILD-01／02 後：tests 45 pass 45）
```

- 通過的測試名（對帳用）：
  - T-RNG：01、02
  - T-R：01、02（含 tie）、03、04、05、06、07（含 never-negative）、08、09（含 reject-no-swan）、10、11（含 short-when-deck-empty）、12、13、14、15、16、17、21、22、23、24、25
  - T-INV：1、2、3、4、5、schema-keys
  - T-DATA：00、01、02、03、04、05、06、07（engine-never-reads-ui-balance）、08（no-simplified-chinese）
- **缺少**（分派單／規格要求但檔中無同名字串）：`T-R-18`、`T-R-19-ai-stop-rules`、`T-R-20-ai-buy-most-expensive`、`T-AI-selfplay-stats`、`tests/engine/ai.test.js`。
- selfplay 主表（human 座位 normal 策略代打）：easy AI 勝 0.5%、normal 44.0%（平手 1.0%）、hard 92.0%；爆倉率 0%；每局約 163～226 個 action。

## 4. 未預期發現

- 上述 AI 規則三個測試缺口：AI 決策目前無直接單元測試，只有 selfplay 間接覆蓋。建議由 S3 負責人（或 systems-engineer 另開子任務）補 `tests/engine/ai.test.js`；接手者依紀律未越界補寫。
- `T-DATA-07` 的內容（engine 不讀 `balance.ui`）與規格 §9.3 未定義的 07 不同，屬前一位工程師自訂的額外檢查；`T-DATA-08` 同為額外的簡體字掃描。無害，但規格 §9.3 只列到 06。
- `selfplay.js` 的預設 200 局在本機約 1 秒內跑完，可考慮納入 `npm test`（分派單原要求以 `T-AI-selfplay-stats` 掛在 node:test 下）。

## 5. 需要製作人決定的事

- 是否接受 S3 以「部分完成」進入 G4.5，AI 規則測試（T-R-18～20）延到 G5 前補齊；或先補齊再進審查。
