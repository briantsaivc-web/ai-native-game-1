# 外部 AI 審查回覆 2（原文，製作人 2026-09-13 貼回；AI 名稱 UNKNOWN，製作人未註明）

**核心規則看起來可用，但建議先修正兩個會誤導玩家決策的顯示問題，再進行試玩。** 這次是附件程式審查，沒有實際操作完整遊戲畫面。

我從附件擷取程式，完成一局 199 個 action 的驗證：凍結輸入 state 後可正常執行，相同初始設定與 action 序列的結果完全一致。嚴重度依實際影響判定。

| 嚴重度 | 位置 | 問題與驗證情境 | 建議 |
|---|---|---|---|
| Major | `src/ui/app.js:90` | 翻倍收入的籌碼仍顯示原價。擁有「第三口鍋」，前兩顆各收入 10，第三顆為大單時，暫存收入正確變成 100，但籌碼顯示 `＋10、＋10、＋40`，合計只有 60。 | 由 engine 事件提供本顆實際收入，UI 顯示 `＋80` 與翻倍標記。不要在 UI 重算效果。 |
| Major | `src/ui/app.js:221–224` | 距離爆倉的提示未考慮首次黑天鵝退回。擁有「老闆娘的好人緣」、效果尚未使用時，畫面說「再翻 3 顆黑天鵝就爆倉」；驗證連翻黑天鵝，實際第 4 顆才爆倉。 | 改為「黑天鵝值再增加 3 點即爆倉」，另外顯示首次退回效果是否可用。 |
| Minor | `src/ui/app.js:397–402、441–444` | 錯誤牌局代碼會被悄悄換成新代碼。輸入 `4294967296` 或 `abc`，解析失敗後會直接用時間產生 seed。 | 區分「空白」和「格式錯誤」；只有空白才自動產生，錯誤輸入顯示提示。 |

**遊戲體驗上的判斷**
- 後期購買可能失去選擇感：市場只有 12 張資產、雙方共用且不回補牌庫；卡牌買完後購買階段只剩保險或跳過。
- 最後一回合 AI 會做確定虧分的投資（買最貴資產，例如發電機花 180 淨資產只增 130）。符合目前 AI 規格，不是實作 bug，但削弱可信度。

**六項疑慮核對**：Q-1 未發現（深度凍結整局通過）；Q-2 未發現不一致；Q-3 整數；Q-4 有：爆倉剩餘量與市場預覽在 UI 計算（app.js:197、205、221），實際購買仍受 reducer 驗證；Q-5 合法狀態下未發現；Q-6 靜態檢查未發現錯誤。
觸控尺寸、響應式版面與單檔離線執行，因附件省略 CSS、HTML 模板，本次無法獨立確認。
